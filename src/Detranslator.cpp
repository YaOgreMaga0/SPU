#include "../src_h/Detranslator.h"

int Detranslator(char* filenamein, char* filenameout)
{
    FILE* byte = fopen(filenamein, "rb");
    FILE* text = fopen(filenameout, "wb");


}

int FillText(const char* filename, int ProgCount)
{
    int d =
}
